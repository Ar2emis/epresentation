import { data as codeQuality } from './code_quality.yml'
import { data as jets } from './jets.yml'

const PRESENTATIONS = {
  [codeQuality.id]: codeQuality,
  [jets.id]: jets
}

export default PRESENTATIONS
